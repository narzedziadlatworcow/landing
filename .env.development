# Konfiguracja lokalna landingu (ładowana przez `astro dev`).
# Wskazuje na lokalnie uruchomioną platformę (`arc platform dev` → :5005).

PUBLIC_MAILING_API_URL=http://localhost:5005

# GTM celowo puste lokalnie — analityka i tak odpala się tylko w buildzie PROD.
PUBLIC_GTM_ID=
